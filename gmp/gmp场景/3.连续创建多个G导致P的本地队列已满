假设某个P的本地队列最多90个G,然后这个P有个正在执行的G创建了99个G
创建的G优先加到本地队列中
如果本地队列满了,会随机在本地队列划一条线,随机将前面的G放到全局队列中,同样这时候新创建的G也是放到全局队列中,最后本地队列中的剩下G往前面移动,这时候本地队列不是满的,所以这时候再有新的G创建就会放进本地队列中
这样设计其实也是为了后面的G能够有更大的执行机会,所谓的给新人机会
其实P的本地队列最多存放256个G

全局队列是没有顺序的,因为全局的G很可能会被不同的M执行,这样的设计可以降低不同的M同时修改同意缓存行

